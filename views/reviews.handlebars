{{!-- grab reviews data --}}
{{#each reviews as |review| }}
{{user.name}}
{{reivew.date_created}}
{{review.content}}
{{/each}}

{{!-- grab movies data --}}
{{#each movies as |movie| }}
{{/each}}


{{!-- render search data if user searched --}}
<div class="hero is-ancestor">
  <div class="hero-body is-7 is-vertical is-parent">
    <div class=" tile is-child box">
      <h1 class="title has-text-centered" ><b>Movie name</b></h1>
          <p class=" title">Reviews:</p>
          <p>actual reviews</p>
          <p>actual reviews</p>
          <div class="field">
            <div class="control">
              <textarea class="textarea" placeholder="Normal textarea"></textarea>
              <button>Post</button>
              <button>Update</button>
              <button>Delete</button>
            </div>
        </div>
    </div>
  </div>
</div>
