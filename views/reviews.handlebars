{{!-- render search data if user searched --}}
<div class="hero is-dark is-fullheight">
  <div class="hero-body is-7 is-vertical is-parent">
    <div class=" tile is-child box">
      {{#each movies as |movie| }}
      <h1 class="is-size-2 has-text-centered" ><b>{{original_title}}</b></h1>
        <img id="movieImage" src="https://image.tmdb.org/t/p/w342/{{poster_path}}">
      {{/each}}
          <p class="is-size-4"><b>Reviews:</b></p>
          <article class="message is-info">
          {{#each reviews as |review| }}
            <div class="message-body">
              <p>{{user.name}}</p>
              <p>{{review.date_created}}</p>
              <p>{{review.content}}</p>
              <button class="button is-normal is-responsive is-info is-outlined" id="delete-review" data-id="{{review.id}}">Delete</button>

            </div>
          {{/each}}
          </article>
          
{{!-- 
  {{#if reviews.length}}
  <div class="col-md-6 project-list">
    <p class="is-size-4"><b>Reviews:</b></p>

    {{#each reviews as |review|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <p>{{user.name}}</p>
        <p>{{review.date_created}}</p>
        <p>{{review.content}}</p>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
{{/if}} --}}



          <div class="field">
            <div class="control">
              <textarea class="textarea is-info" id="review-input" placeholder="Your movie review"></textarea>
              <button class="button is-normal is-responsive is-info is-outlined" id="post-review">Post</button>
              {{!-- <button class="button is-normal is-responsive is-info is-outlined" id="update-review" data-id="{{review.id}}">Update</button> --}}
              {{!-- <button class="button is-normal is-responsive is-info is-outlined" id="delete-review" data-id="{{review.id}}">Delete</button> --}}
            </div>
        </div>
    </div>
  </div>
</div>

<script src="/js/reviews.js"></script>
